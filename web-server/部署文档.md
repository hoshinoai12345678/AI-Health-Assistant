# AI大健康助手 - Web服务器版部署文档

## 📋 目录

- [项目概述](#项目概述)
- [系统要求](#系统要求)
- [快速开始（一键部署）](#快速开始一键部署)
- [CentOS服务器部署](#centos服务器部署)
- [详细部署步骤](#详细部署步骤)
- [配置说明](#配置说明)
- [生产环境部署](#生产环境部署)
- [常见问题](#常见问题)
- [维护与监控](#维护与监控)

---

## 项目概述

AI大健康助手Web服务器版是基于微信小程序版本开发的Web应用，提供与小程序相同的功能和界面体验。

### 主要特性

- ✅ **完整功能**: 包含AI咨询、对话历史、个人中心等所有功能
- ✅ **界面一致**: 与微信小程序保持相同的UI/UX设计
- ✅ **独立部署**: 单独的文件夹结构，不影响小程序代码
- ✅ **响应式设计**: 支持PC和移动端访问
- ✅ **API复用**: 复用后端API服务，保持数据一致性
- ✅ **一键部署**: 自动安装所有依赖组件，开箱即用

### 技术栈

- **前端**: HTML5 + CSS3 + JavaScript (原生)
- **后端**: FastAPI + Python 3.8+
- **数据库**: PostgreSQL
- **缓存**: Redis
- **AI服务**: 阿里云通义千问 / OpenAI

---

## 系统要求

### 硬件要求

- **CPU**: 2核心及以上
- **内存**: 4GB及以上
- **磁盘**: 20GB可用空间

### 软件要求

- **操作系统**: CentOS 7/8, Ubuntu 18.04+, Windows 10+, macOS 10.14+
- **Python**: 3.8 或更高版本（脚本会自动安装）
- **PostgreSQL**: 12.0 或更高版本（脚本会自动安装）
- **Redis**: 6.0 或更高版本（脚本会自动安装）

### 网络要求

- 开放端口: 9000 (Web服务器)
- 如需外网访问，需配置防火墙和域名

---

## 快速开始（一键部署）

### 🚀 CentOS服务器（推荐）

**一键部署脚本会自动完成以下操作：**
- ✅ 安装Python 3.8+
- ✅ 安装并配置PostgreSQL数据库
- ✅ 安装并配置Redis缓存
- ✅ 配置防火墙规则
- ✅ 创建Python虚拟环境
- ✅ 安装所有依赖包
- ✅ 初始化数据库
- ✅ 启动Web服务器

**使用方法：**

```bash
# 1. 进入web-server目录
cd /path/to/ctz_project/web-server

# 2. 添加执行权限
chmod +x start.sh

# 3. 运行一键部署脚本（建议使用sudo）
sudo ./start.sh

# 4. 等待安装完成，服务器会自动启动
```

**默认配置：**
- 数据库用户: `postgres`
- 数据库密码: `123456`
- Redis密码: `123456`（可选）
- API Key: `sk-a67e8c874a694d48a81b72dcdebeb045`（阿里云通义千问）
- Web端口: `9000`

**访问地址：**
- 本地访问: `http://localhost:9000`
- 远程访问: `http://服务器IP:9000`

### Windows系统

1. **进入web-server目录**
```bash
cd 项目/web-server
```

2. **运行启动脚本**
```bash
start.bat
```

3. **访问应用**
打开浏览器访问: http://localhost:9000

### Linux/macOS系统（手动部署）

1. **进入web-server目录**
```bash
cd 项目/web-server
```

2. **添加执行权限**
```bash
chmod +x start.sh
```

3. **运行启动脚本**
```bash
./start.sh
```

4. **访问应用**
打开浏览器访问: http://localhost:9000

---

## CentOS服务器部署

### 方式一：一键部署（推荐）

使用我们提供的自动化部署脚本，无需手动安装任何组件：

```bash
# 克隆或上传项目到服务器
cd /opt
git clone <项目地址> ctz_project
# 或使用scp上传: scp -r ctz_project root@服务器IP:/opt/

# 进入web-server目录
cd ctz_project/web-server

# 赋予执行权限
chmod +x start.sh

# 运行一键部署（需要root权限）
sudo ./start.sh
```

**脚本执行流程：**

1. **系统检查** - 检测操作系统和权限
2. **安装系统依赖** - 安装开发工具和基础软件
3. **安装Python** - 自动安装Python 3.8+
4. **安装PostgreSQL** - 安装并初始化数据库
5. **安装Redis** - 安装并配置缓存服务
6. **配置防火墙** - 开放9000端口
7. **配置Python环境** - 创建虚拟环境并安装依赖
8. **配置环境变量** - 自动生成.env配置文件
9. **初始化数据库** - 创建数据表和初始数据
10. **启动服务** - 启动Web服务器

**部署完成后：**

```bash
# 查看服务状态
systemctl status postgresql
systemctl status redis

# 测试数据库连接
psql -U postgres -d health_db -h localhost

# 测试Redis连接
redis-cli ping

# 访问Web服务
curl http://localhost:9000/health
```

### 方式二：手动部署

如果一键脚本遇到问题，可以按照以下步骤手动部署：

#### 1. 更新系统并安装基础工具

```bash
# 更新系统
sudo yum update -y

# 安装开发工具
sudo yum groupinstall -y "Development Tools"
sudo yum install -y wget curl git vim net-tools
```

#### 2. 安装Python 3.8+

```bash
# CentOS 7需要从源码安装或使用IUS仓库
sudo yum install -y python3 python3-pip python3-devel

# 验证安装
python3 --version
pip3 --version
```

#### 3. 安装PostgreSQL

```bash
# 安装PostgreSQL
sudo yum install -y postgresql-server postgresql-contrib

# 初始化数据库
sudo postgresql-setup initdb

# 启动服务
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 配置数据库
sudo -u postgres psql -c "CREATE DATABASE health_db;"
sudo -u postgres psql -c "ALTER USER postgres WITH PASSWORD '123456';"

# 修改认证方式（允许密码登录）
sudo sed -i 's/ident/md5/g' /var/lib/pgsql/data/pg_hba.conf
sudo systemctl restart postgresql
```

#### 4. 安装Redis

```bash
# 安装EPEL仓库
sudo yum install -y epel-release

# 安装Redis
sudo yum install -y redis

# 启动服务
sudo systemctl start redis
sudo systemctl enable redis

# 测试连接
redis-cli ping
```

#### 5. 配置防火墙

```bash
# 开放9000端口
sudo firewall-cmd --permanent --add-port=9000/tcp
sudo firewall-cmd --reload

# 查看开放的端口
sudo firewall-cmd --list-ports
```

#### 6. 部署应用

```bash
# 进入项目目录
cd /opt/ctz_project/web-server

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install --upgrade pip
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 配置环境变量
cp env.example .env
vim .env  # 修改数据库密码等配置

# 初始化数据库
cd ../backend
python scripts/init_db.py
cd ../web-server

# 启动服务
python main.py
```

### 服务器安全配置

#### 1. 修改默认密码

部署完成后，请立即修改默认密码：

```bash
# 修改PostgreSQL密码
sudo -u postgres psql -c "ALTER USER postgres WITH PASSWORD '你的强密码';"

# 修改Redis密码（可选）
sudo vim /etc/redis.conf
# 找到 # requirepass foobared
# 改为 requirepass 你的强密码
sudo systemctl restart redis

# 更新.env文件中的密码
vim .env
```

#### 2. 配置SELinux（如果启用）

```bash
# 检查SELinux状态
getenforce

# 如果是Enforcing，需要配置策略
sudo semanage port -a -t http_port_t -p tcp 9000

# 或临时关闭（不推荐生产环境）
sudo setenforce 0
```

#### 3. 配置系统服务

创建systemd服务文件，实现开机自启：

```bash
sudo vim /etc/systemd/system/health-web.service
```

添加以下内容：

```ini
[Unit]
Description=AI Health Assistant Web Server
After=network.target postgresql.service redis.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/ctz_project/web-server
Environment="PATH=/opt/ctz_project/web-server/venv/bin"
ExecStart=/opt/ctz_project/web-server/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
# 重载systemd配置
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start health-web

# 设置开机自启
sudo systemctl enable health-web

# 查看服务状态
sudo systemctl status health-web

# 查看日志
sudo journalctl -u health-web -f
```

### 使用Nginx反向代理（推荐）

#### 1. 安装Nginx

```bash
sudo yum install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

#### 2. 配置Nginx

```bash
sudo vim /etc/nginx/conf.d/health-web.conf
```

添加以下配置：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 修改为你的域名或IP

    # 访问日志
    access_log /var/log/nginx/health-web-access.log;
    error_log /var/log/nginx/health-web-error.log;

    # 静态文件
    location /static {
        alias /opt/ctz_project/web-server/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 代理到Web服务器
    location / {
        proxy_pass http://127.0.0.1:9000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
```

#### 3. 测试并重启Nginx

```bash
# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx

# 开放80端口
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --reload
```

#### 4. 配置HTTPS（可选但推荐）

```bash
# 安装certbot
sudo yum install -y certbot python3-certbot-nginx

# 获取SSL证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

---

## 配置文件说明

### .env环境变量配置

```bash
# 应用基础配置
APP_NAME=AI大健康助手Web版
DEBUG=False  # 生产环境设置为False
VERSION=1.0.0

# 数据库配置（修改密码）
DATABASE_URL=postgresql+asyncpg://postgres:123456@localhost:5432/health_db

# Redis配置
REDIS_URL=redis://localhost:6379/0
REDIS_EXPIRE_SECONDS=3600

# JWT配置（生产环境必须修改）
SECRET_KEY=your-very-secure-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=7

# AI服务配置（已配置通义千问）
OPENAI_API_KEY=
DASHSCOPE_API_KEY=sk-a67e8c874a694d48a81b72dcdebeb045

# 文件存储配置
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=health-assistant

# 限流配置
RATE_LIMIT_PER_MINUTE=100
```

**注意：** Web服务器的端口配置在 `main.py` 中设置，默认为 9000。如需修改，请编辑 `main.py` 文件。

### 生成安全密钥

```bash
# 使用Python生成随机密钥
python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# 或使用OpenSSL
openssl rand -base64 32
```

---

## 验证部署

### 1. 检查服务状态

```bash
# 检查PostgreSQL
sudo systemctl status postgresql
psql -U postgres -d health_db -h localhost -c "SELECT version();"

# 检查Redis
sudo systemctl status redis
redis-cli ping

# 检查Web服务
curl http://localhost:9000/health
```

### 2. 测试Web界面

打开浏览器访问：
- 本地: http://localhost:9000
- 远程: http://服务器IP:9000

### 3. 测试API接口

```bash
# 健康检查
curl http://localhost:9000/health

# API信息
curl http://localhost:9000/api/info

# 注册测试账号
curl -X POST http://localhost:9000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"123456","email":"test@example.com"}'
```

### 4. 查看日志

```bash
# 查看Web服务日志
sudo journalctl -u health-web -f

# 查看PostgreSQL日志
sudo tail -f /var/lib/pgsql/data/log/postgresql-*.log

# 查看Redis日志
sudo tail -f /var/log/redis/redis.log
```

---

## 性能优化

### 1. 使用Gunicorn多进程

```bash
# 安装gunicorn
pip install gunicorn

# 启动（4个worker进程）
gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:9000

# 根据CPU核心数调整worker数量
# worker数 = (CPU核心数 * 2) + 1
```

### 2. 配置数据库连接池

编辑 `.env` 文件：

```bash
# 数据库连接池配置
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=10
```

### 3. 优化Redis缓存

```bash
# 编辑Redis配置
sudo vim /etc/redis.conf

# 设置最大内存
maxmemory 256mb
maxmemory-policy allkeys-lru

# 重启Redis
sudo systemctl restart redis
```

### 4. 启用Nginx缓存

```nginx
# 在Nginx配置中添加
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=my_cache:10m max_size=1g inactive=60m;

location /static {
    proxy_cache my_cache;
    proxy_cache_valid 200 30d;
}
```

---

## 监控与维护

### 1. 系统监控

```bash
# 安装监控工具
sudo yum install -y htop iotop nethogs

# 查看系统资源
htop

# 查看磁盘IO
iotop

# 查看网络流量
nethogs
```

### 2. 数据库监控

```sql
-- 连接数据库
psql -U postgres -d health_db

-- 查看活动连接
SELECT * FROM pg_stat_activity;

-- 查看数据库大小
SELECT pg_size_pretty(pg_database_size('health_db'));

-- 查看表大小
SELECT 
    schemaname,
    tablename,
    pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS size
FROM pg_tables
ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC
LIMIT 10;
```

### 3. 日志管理

```bash
# 配置日志轮转
sudo vim /etc/logrotate.d/health-web

# 添加以下内容
/var/log/health-web/*.log {
    daily
    rotate 14
    compress
    delaycompress
    notifempty
    create 0640 root root
    sharedscripts
    postrotate
        systemctl reload health-web
    endscript
}
```

### 4. 自动备份

创建备份脚本：

```bash
sudo vim /opt/backup-health-web.sh
```

添加以下内容：

```bash
#!/bin/bash
BACKUP_DIR="/backup/health-web"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

# 备份数据库
pg_dump -U postgres health_db | gzip > $BACKUP_DIR/db_$DATE.sql.gz

# 备份配置文件
tar -czf $BACKUP_DIR/config_$DATE.tar.gz /opt/ctz_project/web-server/.env

# 删除30天前的备份
find $BACKUP_DIR -name "*.gz" -mtime +30 -delete

echo "备份完成: $DATE"
```

设置定时任务：

```bash
# 添加执行权限
sudo chmod +x /opt/backup-health-web.sh

# 编辑crontab
sudo crontab -e

# 添加：每天凌晨2点备份
0 2 * * * /opt/backup-health-web.sh >> /var/log/backup.log 2>&1
```

### 5. 更新部署

```bash
# 停止服务
sudo systemctl stop health-web

# 备份当前版本
cd /opt
sudo tar -czf ctz_project_backup_$(date +%Y%m%d).tar.gz ctz_project/

# 更新代码
cd ctz_project
git pull

# 更新依赖
cd web-server
source venv/bin/activate
pip install -r requirements.txt --upgrade

# 重启服务
sudo systemctl start health-web

# 查看状态
sudo systemctl status health-web
```

---

## 常见问题

### Q1: 一键脚本执行失败

**问题**: 脚本提示权限不足

**解决方法:**
```bash
# 使用sudo运行
sudo ./start.sh

# 或添加执行权限
chmod +x start.sh
sudo ./start.sh
```

### Q2: PostgreSQL安装失败

**问题**: yum找不到postgresql-server包

**解决方法:**
```bash
# CentOS 7需要安装PostgreSQL官方仓库
sudo yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-7-x86_64/pgdg-redhat-repo-latest.noarch.rpm
sudo yum install -y postgresql12-server postgresql12-contrib

# 初始化
sudo /usr/pgsql-12/bin/postgresql-12-setup initdb
sudo systemctl start postgresql-12
sudo systemctl enable postgresql-12
```

### Q3: 数据库连接失败

**问题**: psycopg2.OperationalError: could not connect to server

**解决方法:**
```bash
# 1. 检查PostgreSQL是否运行
sudo systemctl status postgresql

# 2. 修改认证方式
sudo vim /var/lib/pgsql/data/pg_hba.conf
# 将所有 ident 改为 md5

# 3. 重启PostgreSQL
sudo systemctl restart postgresql

# 4. 测试连接
psql -U postgres -d health_db -h localhost
```

### Q4: Redis连接失败

**问题**: redis.exceptions.ConnectionError

**解决方法:**
```bash
# 1. 检查Redis是否运行
sudo systemctl status redis

# 2. 检查Redis配置
sudo vim /etc/redis.conf
# 确保 bind 0.0.0.0 或 bind 127.0.0.1

# 3. 重启Redis
sudo systemctl restart redis

# 4. 测试连接
redis-cli ping
```

### Q5: 端口9000被占用

**问题**: Address already in use

**解决方法:**
```bash
# 查找占用端口的进程
sudo lsof -i :9000
sudo netstat -tulpn | grep 9000

# 杀死进程
sudo kill -9 <PID>

# 或修改端口
vim .env
# 修改 WEB_SERVER_PORT=9001
```

### Q6: 防火墙阻止访问

**问题**: 外网无法访问服务器

**解决方法:**
```bash
# 检查防火墙状态
sudo firewall-cmd --state

# 开放端口
sudo firewall-cmd --permanent --add-port=9000/tcp
sudo firewall-cmd --reload

# 查看已开放端口
sudo firewall-cmd --list-ports

# 如果使用云服务器，还需在控制台配置安全组
```

### Q7: AI服务调用失败

**问题**: API key无效或额度不足

**解决方法:**
```bash
# 1. 检查API key配置
cat .env | grep DASHSCOPE_API_KEY

# 2. 测试API key
curl -X POST https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation \
  -H "Authorization: Bearer sk-a67e8c874a694d48a81b72dcdebeb045" \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen-turbo","input":{"messages":[{"role":"user","content":"你好"}]}}'

# 3. 如果key无效，请到阿里云控制台重新获取
# https://dashscope.console.aliyun.com/
```

### Q8: Python依赖安装失败

**问题**: pip install失败

**解决方法:**
```bash
# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或配置永久镜像源
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple

# 如果缺少编译工具
sudo yum install -y gcc python3-devel
```

### Q9: SELinux阻止服务

**问题**: Permission denied相关错误

**解决方法:**
```bash
# 检查SELinux状态
getenforce

# 临时关闭（测试用）
sudo setenforce 0

# 永久关闭（不推荐）
sudo vim /etc/selinux/config
# 修改 SELINUX=disabled

# 或配置SELinux策略（推荐）
sudo semanage port -a -t http_port_t -p tcp 9000
```

### Q10: 静态文件404

**问题**: 静态资源无法加载

**解决方法:**
```bash
# 检查static目录
ls -la /opt/ctz_project/web-server/static/

# 检查文件权限
sudo chmod -R 755 /opt/ctz_project/web-server/static/

# 如果使用Nginx，检查配置
sudo nginx -t
```

---

## 故障排查

### 查看日志

```bash
# Web服务日志
sudo journalctl -u health-web -f --lines=100

# PostgreSQL日志
sudo tail -f /var/lib/pgsql/data/log/postgresql-*.log

# Redis日志
sudo tail -f /var/log/redis/redis.log

# Nginx日志
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log
```

### 检查服务状态

```bash
# 所有服务状态
sudo systemctl status postgresql redis health-web nginx

# 检查端口监听
sudo netstat -tulpn | grep -E '9000|5432|6379|80'

# 检查进程
ps aux | grep -E 'python|postgres|redis|nginx'
```

### 测试连接

```bash
# 测试数据库
psql -U postgres -d health_db -h localhost -c "SELECT 1;"

# 测试Redis
redis-cli ping

# 测试Web服务
curl -v http://localhost:9000/health

# 测试API
curl -X POST http://localhost:9000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"123456","email":"test@test.com"}'
```

---

## 快速参考

### 常用命令

```bash
# 启动服务
sudo systemctl start health-web

# 停止服务
sudo systemctl stop health-web

# 重启服务
sudo systemctl restart health-web

# 查看状态
sudo systemctl status health-web

# 查看日志
sudo journalctl -u health-web -f

# 进入虚拟环境
cd /opt/ctz_project/web-server
source venv/bin/activate

# 测试健康检查
curl http://localhost:9000/health
```

### 默认配置信息

| 项目 | 值 |
|------|-----|
| Web端口 | 9000 |
| 数据库用户 | postgres |
| 数据库密码 | 123456 |
| 数据库名称 | health_db |
| Redis端口 | 6379 |
| Redis密码 | 无（可选配置123456） |
| API Key | sk-a67e8c874a694d48a81b72dcdebeb045 |

### 重要文件路径

```
/opt/ctz_project/web-server/          # 项目目录
/opt/ctz_project/web-server/.env      # 环境配置
/opt/ctz_project/web-server/venv/     # Python虚拟环境
/etc/systemd/system/health-web.service # 系统服务
/etc/nginx/conf.d/health-web.conf     # Nginx配置
/var/lib/pgsql/data/                  # PostgreSQL数据
/var/log/                             # 日志目录
```

---

## 安全加固建议

### 1. 修改默认密码

```bash
# 修改数据库密码
sudo -u postgres psql -c "ALTER USER postgres WITH PASSWORD '你的强密码';"

# 更新.env配置
vim /opt/ctz_project/web-server/.env
# 修改 DATABASE_URL 中的密码

# 重启服务
sudo systemctl restart health-web
```

### 2. 配置防火墙规则

```bash
# 只允许特定IP访问
sudo firewall-cmd --permanent --add-rich-rule='rule family="ipv4" source address="允许的IP" port protocol="tcp" port="9000" accept'
sudo firewall-cmd --reload

# 或使用Nginx做访问控制
```

### 3. 启用HTTPS

```bash
# 安装certbot
sudo yum install -y certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期
echo "0 3 * * * certbot renew --quiet" | sudo crontab -
```

### 4. 限制数据库访问

```bash
# 编辑pg_hba.conf
sudo vim /var/lib/pgsql/data/pg_hba.conf

# 只允许本地连接
host    health_db    postgres    127.0.0.1/32    md5

# 重启PostgreSQL
sudo systemctl restart postgresql
```

### 5. 配置日志审计

```bash
# 启用详细日志
vim /opt/ctz_project/web-server/.env
# 设置 DEBUG=False

# 配置日志级别
# 在main.py中设置logging.INFO或logging.WARNING
```

---

## 性能基准测试

### 使用Apache Bench测试

```bash
# 安装ab工具
sudo yum install -y httpd-tools

# 测试健康检查接口
ab -n 1000 -c 10 http://localhost:9000/health

# 测试API接口（需要token）
ab -n 100 -c 5 -H "Authorization: Bearer YOUR_TOKEN" http://localhost:9000/api/info
```

### 使用wrk测试

```bash
# 安装wrk
sudo yum install -y wrk

# 测试
wrk -t4 -c100 -d30s http://localhost:9000/health
```

---

## 技术支持

### 文档资源

- [FastAPI官方文档](https://fastapi.tiangolo.com/)
- [PostgreSQL文档](https://www.postgresql.org/docs/)
- [Redis文档](https://redis.io/documentation)
- [阿里云通义千问文档](https://help.aliyun.com/zh/dashscope/)
- [CentOS官方文档](https://docs.centos.org/)

### 在线工具

- [PostgreSQL在线测试](https://www.db-fiddle.com/)
- [Redis在线测试](https://try.redis.io/)
- [正则表达式测试](https://regex101.com/)
- [JSON格式化](https://jsonformatter.org/)

### 社区支持

- GitHub Issues: 提交问题和建议
- Stack Overflow: 搜索技术问题
- CentOS论坛: 系统相关问题

---

## 附录

### A. 目录结构

```
web-server/
├── main.py                 # 主程序入口
├── requirements.txt        # Python依赖
├── env.example            # 环境变量示例
├── .env                   # 环境变量配置（不提交到Git）
├── start.sh               # Linux一键启动脚本
├── start.bat              # Windows启动脚本
├── venv/                  # Python虚拟环境
├── static/                # 静态文件
│   ├── css/
│   │   └── style.css     # 样式文件
│   ├── js/
│   │   └── app.js        # JavaScript文件
│   └── images/           # 图片资源
├── templates/             # HTML模板
│   └── index.html        # 主页面
├── 部署文档.md            # 本文档
└── README.md             # 项目说明
```

### B. 环境变量完整列表

```bash
# 应用配置
APP_NAME=AI大健康助手Web版
DEBUG=False
VERSION=1.0.0

# 数据库配置
DATABASE_URL=postgresql+asyncpg://postgres:123456@localhost:5432/health_db
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=10

# Redis配置
REDIS_URL=redis://localhost:6379/0
REDIS_EXPIRE_SECONDS=3600

# JWT配置
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=7

# 微信小程序配置（Web版不需要）
WECHAT_APP_ID=
WECHAT_APP_SECRET=

# AI服务配置
OPENAI_API_KEY=
DASHSCOPE_API_KEY=sk-a67e8c874a694d48a81b72dcdebeb045

# 文件存储配置
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=health-assistant

# 限流配置
RATE_LIMIT_PER_MINUTE=100
```

**注意：**
- `DATABASE_POOL_SIZE` 和 `DATABASE_MAX_OVERFLOW` 是可选配置，后端会使用默认值
- `LOG_LEVEL` 和 `LOG_FILE` 是可选配置，在 `main.py` 中设置
- Web服务器端口在 `main.py` 中配置，默认为 9000

### C. API接口列表

| 接口 | 方法 | 说明 |
|------|------|------|
| /health | GET | 健康检查 |
| /api/info | GET | API信息 |
| /api/auth/register | POST | 用户注册 |
| /api/auth/login | POST | 用户登录 |
| /api/chat/send | POST | 发送消息 |
| /api/conversation/list | GET | 对话列表 |
| /api/fitness/analyze | POST | 体测分析 |

### D. 数据库表结构

```sql
-- 用户表
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 对话表
CREATE TABLE conversations (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    title VARCHAR(200),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 消息表
CREATE TABLE messages (
    id SERIAL PRIMARY KEY,
    conversation_id INTEGER REFERENCES conversations(id),
    role VARCHAR(20) NOT NULL,
    content TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### E. 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0.0 | 2026-01-19 | 初始版本发布，实现核心功能 |
| v1.0.0 | 2026-01-19 | 添加CentOS一键部署脚本 |
| v1.0.0 | 2026-01-19 | 完善部署文档和故障排查指南 |

---

## 联系方式

如有问题或建议，请通过以下方式联系：

- **项目仓库**: 提交Issue
- **技术支持**: 发送邮件
- **文档反馈**: 提交Pull Request

---

**最后更新**: 2026年1月19日  
**文档版本**: 1.0.0  
**适用系统**: CentOS 7/8, RHEL 7/8

---

**祝您部署顺利！** 🎉
